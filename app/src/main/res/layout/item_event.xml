<?xml version="1.0" encoding="utf-8"?>

<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/eventCardView"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="16dp"
    android:layout_marginEnd="16dp"
    android:layout_marginTop="8dp"
    app:cardBackgroundColor="@color/zircon"
    app:cardCornerRadius="5dp"
    app:cardElevation="0dp"
    app:strokeColor="@color/tropical_blue"
    app:strokeWidth="1dp"
    tools:context=".ui.screen.room_info.RoomInfoFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/event_item_background"
        android:orientation="vertical">

        <TextView
            android:id="@+id/bookedByTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="22dp"
            android:layout_marginTop="14dp"
            android:layout_marginEnd="22dp"
            android:fontFamily="@font/roboto_medium"
            android:letterSpacing="0.02"
            android:lineSpacingExtra="26dp"
            android:singleLine="true"
            android:text="@string/booked_by"
            android:textColor="@color/event_booked_text_color"
            android:textSize="22sp" />

        <TextView
            android:id="@+id/bookIntervalTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="22dp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="14dp"
            android:drawableStart="@drawable/event_item_circle"
            android:drawablePadding="10dp"
            android:letterSpacing="0.02"
            android:lineSpacingExtra="21dp"
            android:text="@string/book_interval"
            android:textColor="@color/event_time_text_color"
            android:textSize="18sp" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>